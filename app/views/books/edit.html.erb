<%= form_for (@book) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :writer %>
	<%= f.text_field :writer %>

	<%= f.label :left %>
	<%= f.text_field :left %>
	<%= f.submit "Update", class: "btn btn-primary" %>

<% end %>
